import { StyleSheet } from 'react-native';

const home = {
  position: 'relative',
  marginTop: 40,
  paddingRight: 20,
  paddingLeft: 20,
  display: 'flex',
  height: 500,
};

export default StyleSheet.create({ home });
