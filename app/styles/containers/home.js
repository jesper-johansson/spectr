import { StyleSheet } from 'react-native';

const home = {
  position: 'relative',
  flexGrow: 1,
  paddingTop: 0,
  display: 'flex',
  backgroundColor: '#f7f7fb',
};

const flatList = {
  paddingTop: 40,
};

export default StyleSheet.create({
  home,
  flatList,
});
