import { StyleSheet } from 'react-native';

const home = {
  position: 'relative',
  flexGrow: 1,
  paddingTop: 40,
  display: 'flex',
  backgroundColor: '#f7f7fb',
};

export default StyleSheet.create({ home });
